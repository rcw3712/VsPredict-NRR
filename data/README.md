# Data not distributed

The pipeline expects authorized copies of `Well-A.xlsx` and `Well-B.xlsx` in this directory. These well logs are proprietary and are intentionally excluded from Git.

Required column mappings and units are defined in `../config/config_nrr_v5.m`. Do not commit raw, processed, or row-level well data.
